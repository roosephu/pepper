import Vue from "vue";
import * as VueRouter from "vue-router";

Vue.use(VueRouter);

export default new VueRouter({
    routes: [
        // {
        //   path: '*',
        //   redirect: '/'
        // }
    ],
});
